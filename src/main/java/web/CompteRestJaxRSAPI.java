package web;

// Cette classe pourrait servir à définir des endpoints pour une API REST basée sur JAX-RS pour la gestion des comptes.
// Cependant, dans sa forme actuelle, elle est vide et ne contient pas de logique spécifique.

public class CompteRestJaxRSAPI {
    // En l'état, cette classe ne fait rien. Elle pourrait être utilisée pour définir des méthodes
    // qui gèrent les requêtes REST liées aux opérations de gestion des comptes (création, lecture, mise à jour, suppression).
    // Par exemple, des méthodes annotées avec @GET, @POST, @PUT, @DELETE pour répondre aux requêtes HTTP.

    // Pour l'instant, elle est vide et ne contient aucun code spécifique lié à JAX-RS ou à la manipulation des comptes.
}
